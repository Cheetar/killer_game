from django.test import TestCase

# TODO check if killing works well e.g. kills counter incrises, victim is dead
""" TODO after two kills in a row no bugs in getting the killer's player
    object (two players with the same current_target)
"""
# TODO game ends if there are two players alive
# TODO after killing a victim killer takes the current_target of victim
# TODO check if kill chain is a cycle
# Create your tests here.
